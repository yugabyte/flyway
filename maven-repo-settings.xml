<settings>
    <servers>
        <server>
            <id>${env.FLYWAY_REPO_ID}</id>
            <username>${env.FLYWAY_REPO_USERNAME}</username>
            <password>${env.FLYWAY_REPO_PASSWORD}</password>
        </server>
    </servers>
</settings>